import { StyleSheet } from "react-native";

export default buttonStyle = StyleSheet.create({

    button : {
        position:'absolute',
        width: 'auto',
        height: 'auto',
        borderRadius : 100,
        left : 10,
        top : 10,
        bordeColor: '#FF0000',
        borderWidth: 1
    }

})